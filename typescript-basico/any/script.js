"use strict";
function normalizar(texto) {
    return texto.trim().toLowerCase();
}
function normalizarString(texto) {
    return texto.trim().toLowerCase();
}
console.log(normalizarString("See W"));
