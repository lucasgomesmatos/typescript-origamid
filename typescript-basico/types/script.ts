const produto: string = "Livro";
const preco: number = 20;


const carro: {
  marca: string;
  portas: number;
} = {
  marca: "Audi",
  portas: 4
}

function somar(a: number, b: number): number {
  return a + b;
}

console.log(somar(52, 52))